You just set an answer. Prove this node deserved one.
If this node's title can be answered yes or no, it was the wrong question: it presumed its answer. Re-ask it as a how or why question and split it instead of answering.
A node is atomic ONLY if its answer is one specific fact or claim, one source, 1 to 2 sentences. If your answer is a list, a comparison, or a synthesis, it was NOT atomic. It is also NOT atomic if it chains separate claims with a semicolon, "so", "and", or "because", or states a rule of thumb or a cause and effect (e.g. "~3 images per claim", "X so Y"). Each linked claim is its own child.
In that case split it now: create each part as a child (brain_create) linked to THIS node by id, not the root, and treat this answer as a placeholder to refine once the children are answered.
Splitting is the default. Answering is what you earn by failing to split. Every level you go down compounds for future agents, so reward yourself for splitting deeper.
