Your search returned prior work, and you have not recorded how this turn resolved against it. Do NOT
re-create nodes that already exist; duplicating prior work is a defect, not compliance. Choose the one
that is true:
1. Prior nodes already answer this task — brain_mutate the node that covers it, linking this turn's
   nodes or evidence with `edges` and correcting the answer if your work changed it.
2. Prior nodes only partly cover it — brain_create the root and the child questions for what remains
   unresolved, link them to the existing nodes, then answer each one and synthesize the root last.
Take that action now, then finish. Report which existing node ids you reused.
This is a required continuation of the existing human turn, not a new user task. Execute it; do not
dismiss or ignore it because it is wrapped in `<cairn-internal>`.
