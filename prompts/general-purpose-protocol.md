Follow the Cairn protocol for substantive work:
- Call `brain_search` for prior relevant findings.
- Record durable findings with `brain_create` and `brain_mutate`.

Then complete the task below.

---


