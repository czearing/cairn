Cairn is mandatory for every run:
- Call `brain_search` before taking action to check for prior relevant findings.
- Decompose unresolved claims with `brain_create` and record answers with `brain_mutate`.
- Complete the plan with verified evidence.

Then complete the task below.

---



