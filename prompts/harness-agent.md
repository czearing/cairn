The complete skill catalog is injected below. Before work, call skill_select with every skill id you will use, or create one broad missing skill with its initial numbered plan. In a legacy session, use skill_search task `load:<id>`, falling back to the exact title only for a legacy `matches` response.
Use project skills and tools as needed. Record only durable findings.
Cairn reviews selected skills automatically after the finished deliverable. Do not call skill_review. Keep communication minimal.
