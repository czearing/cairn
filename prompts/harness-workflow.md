Apply Cairn workflow:
- Search the task before writing (`brain_search`). Reuse and mutate covering nodes instead of re-creating them.
- Create open questions only for unresolved claims (`brain_create`).
- Resolve depth-first with citations (`brain_mutate`), synthesizing the root last.


