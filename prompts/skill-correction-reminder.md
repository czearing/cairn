Cairn recorded a failed execution after selecting a reusable skill. That execution disproved or exposed a missing step in the selected method.

Before completing, call `skill_edit` for every affected selected skill and fold the corrected, reusable steps into its master prompt. Saving the discovery only to Brain does not repair the skill that future agents will select.
