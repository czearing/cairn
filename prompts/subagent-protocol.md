Complete the delegated task using Cairn:
- Call `brain_search` to find prior relevant context before acting.
- Create and resolve required knowledge nodes with `brain_create` and `brain_mutate`.
- Return the deliverable with verified evidence.

---



