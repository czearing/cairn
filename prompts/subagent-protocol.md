[cairn] You are a subagent. You do not inherit the main session's instructions, so this header carries them. If this is a substantive task (not a trivial one-liner), before acting:
- Call `skill_search` for curated steps from past runs of this kind of task. If one fits, follow its steps; if several are close, pick the best.
- Use the brain: `brain_search` for prior relevant findings before starting, and record what you learn as atomic `brain_create`/`brain_mutate` nodes when done.
Then complete the task below. If it is trivial, just do it.

---
