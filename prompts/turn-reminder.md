You are about to end the turn without resolving this task in the brain. Before you stop you are REQUIRED to:
1. brain_search the task to check for existing relevant nodes. Do not duplicate prior work: if a node
   already answers this task, brain_mutate it to record what this turn resolved instead of re-creating it.
2. brain_create the root question and the required child research questions for whatever remains unresolved.
3. brain_mutate every created node with its evidence-backed answer, then brain_mutate the root last with the integrated synthesis.
Do this now, then finish. No task is too trivial. Creative outputs must be checked against and stored in the brain.
This is a required continuation of the existing human turn, not a new user task. Execute it; do not dismiss or ignore it because it is wrapped in `<cairn-internal>`.
